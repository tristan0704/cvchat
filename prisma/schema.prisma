datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Cv {
  token      String   @id
  data       Json
  createdAt DateTime @default(now())
}
